{% include "base.html.twig" %}

{# {% block title %}Dice{% endblock %} #}

{% block content %}

<body>
    {% include 'nav.twig' %}
    <div class="container">
        <h2>{{ title }}</h2>

        <div class="deck">
            <div class="column">
                {% for card in card_deck %} <img class="cards" src="{{ asset("../img/cards/" ~ card ~ ".svg") }}" width="100" height="140" alt="{{card}}">
                {# <i class="card-sprite {{card}}">{{card}}</i> #}
                {% endfor %}
            </div>
        </div>
    </div>

    {% include 'footer.twig' %}
</body>

{% endblock %}